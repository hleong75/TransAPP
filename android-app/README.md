# Android TransAPP

Ce module fournit un squelette Android minimal pour TransAPP. La logique métier reste dans le package Python en attendant un portage natif.

## Lancer l'app

```bash
./gradlew :android-app:assembleDebug
```
